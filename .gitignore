.claude/
.DS_Store

# Smoke-test artifacts produced when running DDW skills against the plugin repo itself.
# The plugin doesn't ship a top-level logs/ or audits/ — those are consumer-side artifacts.
# (templates/logs/ is shipped and tracked.)
/logs/
/audits/
